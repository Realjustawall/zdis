Create `metrics-token.txt` in this directory before starting the enterprise
Compose stack. It must contain one long random token and no other text.

The directory is ignored by Git; only this README is tracked.
